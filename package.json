{
  "name": "payment_app_project",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:generate":"cd packages/db && npx prisma generate && cd ../..",
    "start-user-app":"cd ./apps/user_app && npm run start",
    "start-webhook" : "cd ./apps/bank-webhook-express && pm2-runtime start ./dist/index.js"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.5.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
